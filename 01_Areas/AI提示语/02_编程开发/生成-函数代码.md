---
title: 生成-函数代码
description: 根据需求生成高质量函数代码的提示语
用途: 代码生成
适用模型: GPT-4 / Claude / Copilot
分类: 编程开发
难度: 初级
创建日期: 2026-02-10
更新日期: 2026-02-10
使用次数: 0
评分: ⭐⭐⭐⭐⭐
状态: 已验证
tags:
  - prompt
  - 编程
  - 代码生成
  - favorite
---

# 生成-函数代码

---

## 📋 元数据

| 属性 | 值 |
|------|-----|
| **用途** | 根据需求生成高质量函数代码 |
| **适用模型** | GPT-4 / Claude / Copilot |
| **分类** | 编程开发 |
| **难度** | 初级 |
| **创建日期** | 2026-02-10 |
| **更新日期** | 2026-02-10 |
| **使用次数** | 0 |
| **评分** | ⭐⭐⭐⭐⭐ |
| **状态** | 已验证 |

---

## 📝 提示语内容

```
你是一位资深软件工程师，擅长编写简洁、高效、可维护的代码。

请根据以下需求生成代码：

## 需求描述
{{功能描述}}

## 技术要求
- **编程语言**: {{语言}}
- **函数签名**: {{函数签名}}
- **输入参数**: 
  {{参数列表}}
- **返回值**: 
  {{返回值说明}}
- **约束条件**: 
  {{约束条件}}

## 代码规范
1. 添加清晰的中文注释
2. 使用有意义的变量名
3. 处理边界情况和异常
4. 遵循语言的最佳实践
5. 保持代码简洁，避免过度设计

## 输出要求
请提供：
1. **函数实现** - 完整的可运行代码
2. **使用示例** - 3个不同场景的调用示例
3. **复杂度分析** - 时间复杂度和空间复杂度
4. **测试用例** - 包含正常和边界情况

开始生成代码：
```

---

## 🎯 使用说明

### 适用场景
- 快速生成工具函数
- 算法实现
- 数据处理逻辑
- API 封装函数

### 输入变量
| 变量名 | 说明 | 示例 |
|--------|------|------|
| `功能描述` | 函数要实现的详细功能 | 实现一个防抖函数 |
| `语言` | 编程语言 | JavaScript / Python |
| `函数签名` | 函数定义 | function debounce(fn, delay) |
| `参数列表` | 输入参数说明 | fn: 要防抖的函数, delay: 延迟时间 |
| `返回值` | 返回值说明 | 返回防抖后的函数 |
| `约束条件` | 特殊要求 | 支持取消功能、立即执行选项 |

### 参数设置
- **Temperature**: 0.3-0.5（保证代码准确性）
- **Max Tokens**: 2000-3000

---

## 💡 使用示例

### 示例 1：JavaScript 防抖函数

**输入：**
```
功能描述：实现一个防抖函数，用于控制高频事件的触发频率
语言：JavaScript
函数签名：function debounce(fn, delay, immediate = false)
参数列表：
  - fn: 要防抖的函数
  - delay: 延迟时间（毫秒）
  - immediate: 是否立即执行
返回值：返回防抖处理后的函数
约束条件：支持取消功能，支持返回值
```

**输出：**
```javascript
/**
 * 防抖函数
 * @param {Function} fn - 要防抖的函数
 * @param {number} delay - 延迟时间（毫秒）
 * @param {boolean} immediate - 是否立即执行
 * @returns {Function} 防抖处理后的函数
 */
function debounce(fn, delay, immediate = false) {
  let timeoutId;
  let result;
  
  const debounced = function(...args) {
    const context = this;
    
    if (timeoutId) clearTimeout(timeoutId);
    
    if (immediate) {
      const callNow = !timeoutId;
      timeoutId = setTimeout(() => {
        timeoutId = null;
      }, delay);
      if (callNow) result = fn.apply(context, args);
    } else {
      timeoutId = setTimeout(() => {
        result = fn.apply(context, args);
      }, delay);
    }
    
    return result;
  };
  
  debounced.cancel = function() {
    clearTimeout(timeoutId);
    timeoutId = null;
  };
  
  return debounced;
}

// 使用示例
const handleResize = debounce(() => {
  console.log('Window resized');
}, 300);

window.addEventListener('resize', handleResize);

// 测试用例
// 1. 正常防抖
// 2. 立即执行模式
// 3. 取消功能
```

---

## 🔧 优化记录

### 版本历史

#### v1.0 (2026-02-10)
- 初始版本
- 包含完整的代码规范要求
- 提供测试用例生成

---

## ⚠️ 注意事项

- 生成代码后务必进行测试
- 复杂逻辑需要人工 review
- 注意检查边界情况处理
- 生产环境使用需经过充分测试

---

## 🔗 相关链接

- [[生成-单元测试]]
- [[审查-代码质量]]
- [[解释-算法原理]]

---

> [!success] 效果评估
> - **准确性**: ⭐⭐⭐⭐⭐
> - **实用性**: ⭐⭐⭐⭐⭐
> - **稳定性**: ⭐⭐⭐⭐⭐
> - **综合评分**: ⭐⭐⭐⭐⭐
