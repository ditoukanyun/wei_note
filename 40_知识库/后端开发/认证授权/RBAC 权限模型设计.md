---
type: wiki
area: "[[后端开发]]"
tags:
  - 后端开发
  - RBAC
  - 权限
created: 2026-05-08
---
# RBAC 权限模型设计

## 定义

RBAC 是基于角色的访问控制模型，通过用户、角色、权限之间的关系管理系统访问能力。

## 要点

- 用户被分配角色，角色拥有权限。
- 权限通常表达为资源和动作的组合。
- 角色层级和数据范围会显著增加复杂度。
- 权限变更需要审计和最小权限原则。

## 模型结构

```mermaid
flowchart LR
    U["用户 User"] --> UR["用户-角色关系"]
    UR --> R["角色 Role"]
    R --> RP["角色-权限关系"]
    RP --> P["权限 Permission"]
    P --> A["资源 + 动作"]
```

## 案例

后台订单系统可以定义：

- 角色：客服、运营、管理员。
- 权限：`order:read`、`order:refund`、`order:export`。
- 数据范围：客服只能看自己负责店铺，管理员可看全部。

如果只做角色判断，不做数据范围校验，用户可能通过改订单 ID 访问不属于自己的资源，形成越权。

## 检查清单

- 权限是否表达为资源和动作，而不是只靠页面菜单。
- 是否有数据范围，例如部门、租户、店铺、本人。
- 是否遵守最小权限原则。
- 权限变更是否记录审计日志。
- 服务端是否做资源级校验，前端隐藏按钮不能替代授权。
- 是否和 [[API 安全基础]] 的越权检查一起设计。

## 常见误区

- 只控制菜单显示，不控制 API 访问。
- 角色直接写死在代码里，后续扩展困难。
- 没有数据范围，导致同角色用户互相访问数据。
- 权限变更不审计，问题发生后无法追踪。

## 落地建议

权限编码建议稳定、可读，例如 `order:read`、`order:refund`、`user:disable`。服务端鉴权时不仅判断用户是否有动作权限，还要判断资源是否属于用户的数据范围。

## 相关概念

- [[认证授权总览：Session、JWT、OAuth2 与 OIDC]]
- [[SpringBoot/16-SpringBoot-JWT-RBAC权限控制]]
- [[API 安全基础]]
