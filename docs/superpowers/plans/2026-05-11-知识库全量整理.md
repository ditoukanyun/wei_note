# 知识库全量整理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 按 `99_系统/规范/知识库目录规范.md` 对 `40_知识库` 做全量、低风险、可验证整理。

**Architecture:** 先做不会破坏 wikilink 的机械化整理，再补缺失专题 MOC，最后留下台账继续分批处理。旧文件不批量移动或重命名，所有整理都优先通过 frontmatter、MOC 和入口链接收敛。

**Tech Stack:** Obsidian Flavored Markdown、YAML frontmatter、wikilink、shell/Ruby 机械化文本处理。

---

### Task 1: 全量盘点

**Files:**
- Check: `40_知识库/**/*.md`
- Check: `99_系统/规范/知识库目录规范.md`

- [x] **Step 1: 统计 Markdown 文件总数**

Run: `find 40_知识库 -type f -name '*.md' | wc -l`

Observed: 514 files.

- [x] **Step 2: 统计 frontmatter 缺口**

Observed:

- 缺 `area`：141 个文件。
- 缺 `created`：124 个文件。
- 缺 `tags`：1 个文件。
- 所有 Markdown 文件都有 frontmatter 起始块。

- [x] **Step 3: 统计缺 MOC 的专题目录**

Observed:

- `40_知识库/AI/AI相关/向量数据库`：10 files，缺专题 MOC。
- `40_知识库/后端开发/SpringBoot`：43 files，缺专题 MOC。
- `40_知识库/通用技能/Git`：11 files，缺专题 MOC。
- `40_知识库/通用技能/算法`：10 files，缺专题 MOC。

### Task 2: 机械化 frontmatter 规范化

**Files:**
- Modify: `40_知识库/**/*.md`

- [x] **Step 1: 为缺失字段补齐 frontmatter**

Rules:

- 缺 `area` 时，根据 `40_知识库/<领域>/...` 推断为 `[[领域]]`。
- 顶层文件按文件名推断领域，例如 `40_知识库/后端开发.md` 为 `[[后端开发]]`。
- 缺 `tags` 时补 `tags: []`。
- 缺 `created` 且 frontmatter 有 `date: YYYY-MM-DD` 时，使用同一天补 `created`。
- 缺 `created` 且没有可用 `date` 时，使用本轮整理日期 `2026-05-11`。
- frontmatter 结束行后仍直接接正文标题。

- [x] **Step 2: 验证 frontmatter 缺口归零**

Run the same inventory commands from Task 1.

Expected:

- 缺 `area`：0。
- 缺 `created`：0。
- 缺 `tags`：0。
- frontmatter 后空行问题：0。

### Task 3: 补缺失专题 MOC

**Files:**
- Create: `40_知识库/AI/AI相关/向量数据库/向量数据库 MOC.md`
- Create: `40_知识库/后端开发/SpringBoot/SpringBoot MOC.md`
- Create: `40_知识库/通用技能/Git/Git MOC.md`
- Create: `40_知识库/通用技能/算法/算法 MOC.md`

- [x] **Step 1: 创建向量数据库 MOC**

Include links to `向量数据库`、`Embedding`、`ANN`、`HNSW`、`IVF`、`PQ`、`FAISS`、`Milvus`、`Pinecone`、`Chroma`。

- [x] **Step 2: 创建 SpringBoot MOC**

Group existing numbered SpringBoot notes by initialization, data/cache, auth/security, API/governance, resilience, event-driven, deployment/observability.

- [x] **Step 3: 创建 Git MOC**

Group existing Git notes by basic operations, branch strategy, collaboration, review, worktree.

- [x] **Step 4: 创建算法 MOC**

Group existing algorithm notes by data structures, basic algorithms, dynamic programming, greedy, interview practice.

### Task 4: 更新上级入口

**Files:**
- Modify: `40_知识库/AI/AI MOC.md`
- Modify: `40_知识库/后端开发/后端开发 MOC.md`
- Modify: `40_知识库/通用技能/通用技能 MOC.md`

- [x] **Step 1: 在 AI MOC 中接入向量数据库 MOC**

Replace or supplement direct vector database links with `[[AI相关/向量数据库/向量数据库 MOC|向量数据库 MOC]]`。

- [x] **Step 2: 在后端 MOC 中接入 SpringBoot MOC**

Add `[[SpringBoot/SpringBoot MOC|SpringBoot MOC]]` to language/framework section.

- [x] **Step 3: 在通用技能 MOC 中接入 Git MOC 和算法 MOC**

Add `[[Git/Git MOC|Git MOC]]` and `[[算法/算法 MOC|算法 MOC]]` to relevant sections.

### Task 5: 验证和台账

**Files:**
- Create: `40_知识库/知识库整理台账.md`
- Check: `40_知识库/**/*.md`

- [x] **Step 1: 创建整理台账**

Record:

- 本轮已完成事项。
- 仍不做的事项：批量移动、批量重命名、合并重复文件。
- 下一批建议：按领域逐步处理重复入口和资源归位。

- [x] **Step 2: 最终验证**

Run:

```bash
find 40_知识库 -type f -name '*.md' | wc -l
rg -n "TBD|TODO|待定|占位" 40_知识库 docs/superpowers/plans/2026-05-11-知识库全量整理.md
git status --short
```

Expected:

- Markdown 文件总数可解释。
- 没有新增占位词。
- git status 只包含本轮整理文件和既有未处理文件。
