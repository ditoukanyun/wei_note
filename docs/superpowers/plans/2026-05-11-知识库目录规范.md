# 知识库目录规范 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 根据 `docs/superpowers/specs/2026-05-11-知识库目录规范-design.md` 创建知识库正式目录规范和入口摘要。

**Architecture:** 使用“主规范 + 入口摘要”双文件结构。正式规则集中在 `99_系统/规范/知识库目录规范.md`，`40_知识库/知识库目录规范.md` 只保留日常入口、常用规则和跳转链接，避免两份文件长期冲突。

**Tech Stack:** Obsidian Flavored Markdown、YAML frontmatter、wikilink。

---

### Task 1: 创建正式规范文件

**Files:**
- Create: `99_系统/规范/知识库目录规范.md`

- [x] **Step 1: 创建目录**

Run: `mkdir -p 99_系统/规范`

Expected: `99_系统/规范` 存在。

- [x] **Step 2: 写入正式规范**

Use `apply_patch` to create `99_系统/规范/知识库目录规范.md` with:

```markdown
---
area: [[系统]]
tags:
  - 知识库
  - 规范
  - 目录治理
created: 2026-05-11
---
# 知识库目录规范
```

Then include these sections:

- 规范目标
- 核心原则
- 目录职责
- 命名规则
- MOC 规则
- frontmatter 规则
- 新增知识判断流程
- 渐进整理规则
- 待补知识点清单
- 维护检查清单

- [x] **Step 3: 验证正式规范内容**

Run: `sed -n '1,260p' 99_系统/规范/知识库目录规范.md`

Expected:
- frontmatter 后直接跟正文标题，没有多余空行。
- 文件包含“待补知识点清单”。
- 文件说明正式规范是唯一规则来源。

### Task 2: 创建知识库入口摘要

**Files:**
- Create: `40_知识库/知识库目录规范.md`

- [x] **Step 1: 写入入口摘要**

Use `apply_patch` to create `40_知识库/知识库目录规范.md` with:

```markdown
---
area: [[知识库]]
tags:
  - 知识库
  - 规范
  - 入口
created: 2026-05-11
---
# 知识库目录规范
```

Then include:

- 正式规范链接
- 快速规则
- 新增知识判断
- 主要领域入口
- 待补知识点提示
- 日常维护提醒

- [x] **Step 2: 验证入口摘要不是第二份完整规范**

Run: `wc -l 40_知识库/知识库目录规范.md 99_系统/规范/知识库目录规范.md`

Expected:
- 入口摘要明显短于正式规范。
- 入口摘要链接到正式规范。

### Task 3: 验证范围和 Obsidian 格式

**Files:**
- Check: `99_系统/规范/知识库目录规范.md`
- Check: `40_知识库/知识库目录规范.md`
- Check: `docs/superpowers/plans/2026-05-11-知识库目录规范.md`

- [x] **Step 1: 检查 frontmatter 后空行**

Run:

```bash
awk 'BEGIN{RS="---\n"; ORS=""} NR==2{print substr($0,1,1)}' 99_系统/规范/知识库目录规范.md
awk 'BEGIN{RS="---\n"; ORS=""} NR==2{print substr($0,1,1)}' 40_知识库/知识库目录规范.md
```

Expected: 两个命令都输出 `#`。

- [x] **Step 2: 检查关键链接和章节**

Run:

```bash
rg -n "正式规范|待补知识点清单|\\[\\[99_系统/规范/知识库目录规范\\|知识库目录规范\\]\\]|后端开发 MOC|前端开发 MOC|MOC-Python编程" 99_系统/规范/知识库目录规范.md 40_知识库/知识库目录规范.md
```

Expected: 能找到正式规范、入口链接、待补知识点清单和主要 MOC 链接。

- [x] **Step 3: 检查 git 范围**

Run: `git status --short`

Expected:
- 新增 `docs/superpowers/plans/2026-05-11-知识库目录规范.md`
- 新增 `99_系统/规范/知识库目录规范.md`
- 新增 `40_知识库/知识库目录规范.md`
- 既有 `.obsidian/workspace.json` 和 newsletter 文件仍保持原样，未被本任务修改。
